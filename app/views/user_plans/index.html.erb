<%= render PageHeaderComponent.new(pretitle: "plan selection", title: "Choose a plan") %>
<div class="row">
  <% @plans.each do |plan| %>
    <div class="col-12 col-md-6 col-lg-3">
      <%= render PricingCardComponent.new(plan: plan, user: current_user) %>
    </div>
  <% end %>
</div>
